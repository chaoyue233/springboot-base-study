<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="chaoyue.study.mapper.UserXmlMapper">

    <select id="findAll" resultType="chaoyue.study.entity.User">
        SELECT * FROM demo.springboot_study_user
    </select>
</mapper>